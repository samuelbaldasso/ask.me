{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": "npx tsx src/db/migrateClickEventType.ts && npx prisma db push --accept-data-loss --skip-generate && npx tsx src/db/restorePostgis.ts"
  }
}
